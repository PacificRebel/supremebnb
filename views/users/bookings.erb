<!DOCTYPE html>
 <head>
   <meta charset="utf-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <title>Bookings</title>
   <meta name="description" content="">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
 </head>
 <body>
   <div class="container">
  <div class="row">
    <div class="col">

    </div>
    <div class="col-15">
      <h2>Your Bookings</h2>
    </div>
    <div class="col">

    </div>
  </div>
  <div class="row">
    <div class="col">

    </div>
    <div class="col-5">
      <ul class= 'list-group'>
        <% @bookings.each do |booking| %>
          <li class='list-group-item d-flex justify-content-between align-items-center p-3 mb-2 bg-light text-dark' id="booking-<%= booking.id %>">
            <h5 class="mb-1"><%= booking.space.name %></h5>
            <h6 class="mb-1"><%= booking.date %></h6>
            <h6 class="mb-1"><%= booking.guest.username %></h6>
            <% unless booking.approved? %>
            <form action="/spaces/<%= booking.space_id %>/bookings/<%= booking.id %>" method="post">
              <input type='hidden' name='_method' value='PATCH' />
              <input class='btn btn-success' type="submit" value="Approve" />
            </form>
            <form>
            <input class='btn btn-danger' type="submit" value="Deny" />

          </form>
          <%else%>
    <h5> Already approved! </h5>

          <%end%>
          </li>
          <% end %>
      </ul>
    </div>
    <div class="col">

    </div>
  </div>
   </div>
   <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" async defer></script>
   <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.bundle.min.js" async defer></script>
 </body>
</html>
